#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*
    猴子吃桃问题：猴子第一天摘下若干个桃子，
    当即吃了一半，还不过瘾，又多吃了一个，
    第二天早上又将剩下的桃子吃掉一半，又多
    吃了一个。以后每天早上都吃了前一天剩下
    的一半零一个，到第10天早上想再吃时，见
    只剩下一个桃子了。求第一天共摘了多少。
    */
    int i;    //天数
    int num;  //桃子数
    i = 1;
    num = 1;

    //计算桃子数
    while(i <= 9)
    {
        num = 2 * (num + 1);
        i++;
    }
    printf("猴子第一天摘的桃子数为：%d",num);
    return 0;
}
